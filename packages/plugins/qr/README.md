# Cli extension

The `@qodalis/cli-qr` package is a CLI extension designed to provide utilities to generate a qr code.

# Installation

```bash
packages add @qodalis/cli-qr
```

# Usage

```bash
qr generate --text="Hello world!"
```

This command downloads and registers the extension for use within the CLI environment.
