{
  "name": "electron-cli",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/electron-cli/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["tsup", "cp package.json dist/package.json"],
        "cwd": "packages/electron-cli",
        "parallel": false
      },
      "inputs": ["production", "sharedGlobals"],
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
